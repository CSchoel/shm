within SHM.SchoelzelThesis.Components.Contraction.Unidirectional;
package ManualDelay
annotation(Documentation(info="<html>
  <p>Contains models for delay components that use a manual delay
  implementation.</p>
</html>"));
end ManualDelay;
